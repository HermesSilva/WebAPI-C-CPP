{
    "name": "tootega-webapi-frontend",
    "version": "1.0.0",
    "description": "Frontend for Tootega WebAPI",
    "scripts": {
        "build": "tsc",
        "watch": "tsc --watch",
        "clean": "rmdir /s /q js 2>nul || true"
    },
    "devDependencies": {
        "typescript": "^5.3.0"
    }
}